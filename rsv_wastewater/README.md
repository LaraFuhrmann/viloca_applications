Install conda enviroments with

`snakemake --conda-create-envs-only --use-conda -c1 --rerun-incomplete`


Submit workflow to Slurm cluster with:

`./run_pipeline.sh`

This workflow runs on snakemake v7.32.1.
